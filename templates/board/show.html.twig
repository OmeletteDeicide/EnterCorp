<<<<<<< Updated upstream
{% extends 'base.html.twig' %}

{% block title %}Hello CategoryController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>


<h1 class="m-auto">Bienvenu dans la categorie : {{ board.Name }} </h1>
<a href="{{ path('app_form_board', {'categoryId': board.Id })}}">Créer un board</a>
<div class="container">
{% for subject in subjects %}
    <a href="{{ path('app_subject', {'Id': board.Id })}}"class"block-board">
    <div class="block-title">{{ subjects.Name }}</div>
    </a>
    
{% endfor %}
</div>
=======
{% extends 'index/index.html.twig' %}

{% block title %}Hello CategoryController!
{% endblock %}


{% block dashoborad %}
	<h4 class="item-display">
		Category :
		<a href="{{path("category_show", {"id": category.id})}}">{{ category.Name }}</a>
		<a class="delete" href="/">x</a>
	</h4>

	<h4 class="item-display">
		Board :<a href="{{path("board_show", {"id": board.id})}}">{{ board.Name }}</a>
		<a class="delete" href="{{path("category_show", {"id": category.id})}}">x</a>
	</h4>
	<h4 class="item-display">
		Subject :
	</h4>

{% endblock %}


{% block data %}

	<button class="float-end btn btn-success">Add Subject</button>

	<h3 class="mb-4">Select Subject</h3>


	<table class="table table-striped mt-5">
		<thead>
			<tr>
				<th scope="col">#</th>
				<th scope="col">Name Subject</th>
				<th scope="col">Number of Message</th>
				<th scope="col">Created By</th>

			</tr>
		</thead>
		<tbody>
			{% for subject in subjects %}

				<tr link="{{path("app_form_board", {"categoryId": 1})}}" class="select-cat">
					<th scope="row">{{1}}</th>
					<td>{{ subject.Name }}</td>
					<td>24</td>
					<td>Florian</td>
				</tr>

			{% endfor %}

		</tbody>
	</table>
>>>>>>> Stashed changes
{% endblock %}
